# bgg-library


```
sqlite3 marco.db
CREATE TABLE games(id int, username text, gamename text);
CREATE TABLE users(id INTEGER PRIMARY KEY, username TEXT NOT NULL, whereabouts TEXT NOT NULL, time timestamp);
```